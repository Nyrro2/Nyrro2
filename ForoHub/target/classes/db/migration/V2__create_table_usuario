create table usuario(

     id bigint not null auto_increment,
     nombre varchar(100) not null,
     email varchar(100) not null unique,
     psw varchar(300) not null unique,
     perfil varchar() not null,
     status smallint,

     primary key(id)


)